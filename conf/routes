# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# App
GET     /diseases                   controllers.Application.diseasesPage
GET     /new-issue                  controllers.Application.newIssuePage
GET     /disease                    controllers.Application.diseasesDetailPage

# User authentication
GET     /login                      controllers.Authentication.loginModal
GET     /register                   controllers.Authentication.registerModal

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

    